# gompang-survivor
생존게임

function명 규칙 
 대문자로 시작하면 에디터에서 호출
 소문자로 시작하면 c#내부에서 호출

변수명 규칙
_ 는 에디터에서 초기값 세팅


자동 import 추가 (Quick Fix): Ctrl + . (점)
특정 클래스명 아래에 빨간 줄이 생겼을 때, 해당 클래스에 필요한 using 문을 자동으로 추가해주는 메뉴를 엽니다.
import 정리 (Optimize Imports): Shift + Alt + O
사용하지 않는 using 문을 제거하고, 순서를 가나다순으로 깔끔하게 정렬합니다. 